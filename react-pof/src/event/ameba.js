(function() {
  const ameba = createAmeba({
    selector: document.querySelector('.ameba'),
    numPoints: 10,
    centerX: 490, 
    centerY: 490,
    minRadius: 360, 
    maxRadius: 420,
    minDuration: 1, 
    maxDuration: 2
  })

  const createAmeba = function (options) {
    
  }
})()